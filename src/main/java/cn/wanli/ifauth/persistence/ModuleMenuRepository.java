package cn.wanli.ifauth.persistence;

import cn.wanli.ifauth.domain.ModuleMenu;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ModuleMenuRepository extends JpaRepository<ModuleMenu, Long> {
}
